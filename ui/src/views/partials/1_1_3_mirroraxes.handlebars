{{#each inputs}}
  <div>
    <div class="custom-control custom-radio">
      <input type="radio" id="{{id}}" name="mirroracross" class="custom-control-input"
        {{#if checked}}
          checked
        {{/if}}
      />
      <label class="custom-control-label" for="{{id}}">{{label}}</label>
    </div>
  </div>
{{/each}}
